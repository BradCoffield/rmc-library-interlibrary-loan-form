# Copy this file to .env.production (do not commit secrets)
# Values provided here will be used by Gatsby Functions during production builds.

# Third Iron LibKey API token (Bearer). Keep this secret; do NOT prefix with GATSBY_
LIBKEY_API_KEY=your-libkey-api-token-here

# Numerical Library ID assigned by Third Iron (e.g., 222)
LIBKEY_LIBRARY_ID=your-library-id-here
